// This fixture is deliberately excluded from the normal project. The release
// verifier compiles it to prove that the mobile TypeScript gate fails closed.
export const mobileTypecheckSentinel: string = 42;
